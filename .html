<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>商务智能与应用实验 - 重庆工商大学教学平台</title>
    <style>
        :root {
            --primary: #1a5f9e;
            --primary-dark: #0d3b66;
            --primary-light: #e8f2fb;
            --accent: #e8830c;
            --accent-hover: #c9700a;
            --bg: #f5f6f8;
            --white: #ffffff;
            --border: #e0e4e8;
            --text: #2c3e50;
            --text-light: #5a6c7d;
            --text-muted: #8899a6;
            --shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.06);
            --shadow: 0 2px 12px rgba(0, 0, 0, 0.08);
            --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.15);
            --radius: 8px;
            --radius-sm: 5px;
            --transition: 0.25s cubic-bezier(0.4, 0, 0.2, 1);
            --font: 'Segoe UI', 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', sans-serif;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font);
            background: var(--bg);
            color: var(--text);
            line-height: 1.6;
            min-height: 100vh;
            -webkit-font-smoothing: antialiased;
        }

        /* ============ 顶部导航条 ============ */
        .top-bar {
            background: var(--primary-dark);
            color: #fff;
            font-size: 13px;
            padding: 6px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }
        .top-bar-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }
        .top-bar a {
            color: #c8ddf0;
            text-decoration: none;
            font-size: 12px;
            transition: color 0.2s;
        }
        .top-bar a:hover {
            color: #fff;
        }
        .top-bar .user-info {
            display: flex;
            align-items: center;
            gap: 16px;
        }
        .top-bar .user-avatar {
            width: 28px;
            height: 28px;
            border-radius: 50%;
            background: #4a90c4;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 13px;
            font-weight: 600;
            color: #fff;
        }
        .top-bar .logout-btn {
            background: transparent;
            border: 1px solid rgba(255, 255, 255, 0.4);
            color: #fff;
            padding: 4px 14px;
            border-radius: 14px;
            cursor: pointer;
            font-size: 12px;
            transition: all 0.2s;
        }
        .top-bar .logout-btn:hover {
            background: rgba(255, 255, 255, 0.15);
            border-color: rgba(255, 255, 255, 0.7);
        }

        /* ============ Header ============ */
        .header {
            background: var(--primary);
            color: #fff;
            padding: 0;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.15);
        }
        .header-inner {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 24px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            height: 54px;
        }
        .logo-area {
            display: flex;
            align-items: center;
            gap: 12px;
            flex-shrink: 0;
        }
        .logo-icon {
            width: 38px;
            height: 38px;
            background: #fff;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 900;
            color: var(--primary);
            font-size: 18px;
            letter-spacing: -1px;
        }
        .logo-text {
            font-size: 17px;
            font-weight: 700;
            letter-spacing: 0.5px;
            white-space: nowrap;
        }
        .logo-sub {
            font-size: 11px;
            font-weight: 400;
            opacity: 0.85;
            letter-spacing: 0;
        }

        /* 导航 */
        .nav-links {
            display: flex;
            list-style: none;
            gap: 2px;
            height: 100%;
            align-items: stretch;
        }
        .nav-links li {
            display: flex;
            align-items: center;
            position: relative;
        }
        .nav-links li a {
            color: rgba(255, 255, 255, 0.9);
            text-decoration: none;
            padding: 0 18px;
            font-size: 14px;
            font-weight: 500;
            display: flex;
            align-items: center;
            height: 100%;
            transition: all 0.2s;
            border-radius: 4px 4px 0 0;
            white-space: nowrap;
            letter-spacing: 0.3px;
            cursor: pointer;
        }
        .nav-links li a:hover {
            background: rgba(255, 255, 255, 0.1);
            color: #fff;
        }
        .nav-links li a.active {
            background: rgba(255, 255, 255, 0.18);
            color: #fff;
            font-weight: 600;
            border-bottom: 3px solid #ffc107;
        }
        /* 历年真题导航按钮 - 特别突出 */
        .nav-links li a.nav-zhenti {
            background: var(--accent);
            color: #fff;
            font-weight: 700;
            border-radius: 6px;
            margin: 8px 4px;
            padding: 0 20px;
            height: auto;
            display: flex;
            align-items: center;
            gap: 6px;
            box-shadow: 0 2px 8px rgba(232, 131, 12, 0.35);
            animation: gentlePulse 2.5s ease-in-out infinite;
            letter-spacing: 0.5px;
            cursor: pointer;
        }
        .nav-links li a.nav-zhenti:hover {
            background: #f08c1a;
            box-shadow: 0 4px 16px rgba(232, 131, 12, 0.5);
            transform: translateY(-1px);
            animation: none;
        }
        @keyframes gentlePulse {
            0%,
            100% {
                box-shadow: 0 2px 8px rgba(232, 131, 12, 0.35);
            }
            50% {
                box-shadow: 0 4px 20px rgba(232, 131, 12, 0.6);
            }
        }
        .nav-zhenti .zt-icon {
            font-size: 17px;
        }

        /* ============ 主体布局 ============ */
        .main-container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px 24px 40px;
            display: flex;
            gap: 22px;
        }

        /* 左侧 */
        .sidebar-left {
            width: 310px;
            flex-shrink: 0;
            display: flex;
            flex-direction: column;
            gap: 18px;
        }
        .card {
            background: var(--white);
            border-radius: var(--radius);
            box-shadow: var(--shadow-sm);
            border: 1px solid var(--border);
            overflow: hidden;
            transition: var(--transition);
        }
        .card:hover {
            box-shadow: var(--shadow);
        }
        .card-header {
            padding: 14px 18px;
            font-weight: 700;
            font-size: 15px;
            color: var(--primary-dark);
            border-bottom: 1px solid var(--border);
            background: #fafbfc;
            display: flex;
            align-items: center;
            gap: 8px;
            letter-spacing: 0.3px;
        }
        .card-header .icon-dot {
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--primary);
            flex-shrink: 0;
        }
        .card-body {
            padding: 16px 18px;
        }
        .card-body p {
            font-size: 13.5px;
            color: var(--text-light);
            margin-bottom: 8px;
            line-height: 1.7;
        }
        .card-body .read-more {
            color: var(--primary);
            font-size: 13px;
            cursor: pointer;
            text-decoration: none;
            font-weight: 500;
            display: inline-block;
            margin-top: 4px;
        }
        .card-body .read-more:hover {
            text-decoration: underline;
        }
        .teacher-name {
            font-weight: 700;
            font-size: 15px;
            color: var(--text);
            margin-bottom: 2px;
        }
        .teacher-dept {
            font-size: 12.5px;
            color: var(--text-muted);
            margin-bottom: 6px;
        }
        .teacher-bio {
            font-size: 13px;
            color: var(--text-light);
            line-height: 1.7;
        }

        /* 最新动态列表 */
        .news-list {
            list-style: none;
            font-size: 13px;
            color: var(--text-light);
        }
        .news-list li {
            padding: 8px 0;
            border-bottom: 1px dashed #eef1f5;
            cursor: pointer;
            transition: all 0.2s;
            line-height: 1.5;
        }
        .news-list li:hover {
            color: var(--primary);
            padding-left: 6px;
        }
        .news-list li:last-child {
            border-bottom: none;
        }
        .news-list li::before {
            content: '•';
            color: var(--accent);
            font-weight: bold;
            margin-right: 8px;
            font-size: 16px;
        }

        /* 右侧 */
        .content-right {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 18px;
            min-width: 0;
        }

        /* 课程信息统计卡片 */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 14px;
        }
        .stat-item {
            text-align: center;
            padding: 16px 10px;
            background: var(--white);
            border-radius: var(--radius);
            border: 1px solid var(--border);
            box-shadow: var(--shadow-sm);
            transition: var(--transition);
            cursor: default;
        }
        .stat-item:hover {
            box-shadow: var(--shadow);
            transform: translateY(-2px);
        }
        .stat-number {
            font-size: 28px;
            font-weight: 700;
            color: var(--primary-dark);
            letter-spacing: -1px;
        }
        .stat-label {
            font-size: 12px;
            color: var(--text-muted);
            margin-top: 2px;
        }
        .stat-icon {
            font-size: 22px;
            margin-bottom: 4px;
        }

        /* 课程通知 */
        .notice-item {
            padding: 10px 0;
            border-bottom: 1px dashed #eef1f5;
            font-size: 13px;
            color: var(--text-light);
            cursor: pointer;
            transition: all 0.2s;
        }
        .notice-item:hover {
            color: var(--primary);
        }
        .notice-item:last-child {
            border-bottom: none;
        }
        .notice-tag {
            display: inline-block;
            background: #fff3e0;
            color: var(--accent);
            font-size: 11px;
            padding: 2px 8px;
            border-radius: 10px;
            margin-right: 6px;
            font-weight: 600;
        }

        /* ============ 历年真题功能卡片（右侧醒目位置） ============ */
        .zhenti-feature-card {
            background: linear-gradient(135deg, #fffdf7 0%, #fff8ec 30%, #fff3e0 100%);
            border: 2px solid #f0c78e;
            border-radius: var(--radius);
            padding: 20px 22px;
            cursor: pointer;
            transition: all 0.3s;
            position: relative;
            overflow: hidden;
            box-shadow: 0 2px 10px rgba(232, 131, 12, 0.1);
            animation: subtleGlow 3s ease-in-out infinite;
        }
        @keyframes subtleGlow {
            0%,
            100% {
                box-shadow: 0 2px 10px rgba(232, 131, 12, 0.1);
            }
            50% {
                box-shadow: 0 6px 24px rgba(232, 131, 12, 0.22);
            }
        }
        .zhenti-feature-card:hover {
            border-color: var(--accent);
            transform: translateY(-3px);
            box-shadow: 0 8px 28px rgba(232, 131, 12, 0.28);
            animation: none;
        }
        .zhenti-feature-card::after {
            content: '';
            position: absolute;
            top: -30px;
            right: -30px;
            width: 90px;
            height: 90px;
            background: rgba(232, 131, 12, 0.08);
            border-radius: 50%;
            pointer-events: none;
        }
        .zhenti-feature-content {
            display: flex;
            align-items: center;
            gap: 16px;
            position: relative;
            z-index: 1;
        }
        .zhenti-feature-icon {
            width: 56px;
            height: 56px;
            border-radius: 50%;
            background: var(--accent);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
            flex-shrink: 0;
            box-shadow: 0 4px 14px rgba(232, 131, 12, 0.35);
            color: #fff;
        }
        .zhenti-feature-text h3 {
            font-size: 16px;
            font-weight: 700;
            color: #8b5000;
            margin-bottom: 4px;
        }
        .zhenti-feature-text p {
            font-size: 13px;
            color: #a06820;
            margin: 0;
        }
        .zhenti-feature-arrow {
            margin-left: auto;
            font-size: 22px;
            color: var(--accent);
            flex-shrink: 0;
            transition: transform 0.3s;
        }
        .zhenti-feature-card:hover .zhenti-feature-arrow {
            transform: translateX(6px);
        }
        .zhenti-badge {
            position: absolute;
            top: 10px;
            right: 14px;
            background: #e74c3c;
            color: #fff;
            font-size: 10px;
            font-weight: 700;
            padding: 3px 10px;
            border-radius: 20px;
            z-index: 2;
            letter-spacing: 0.5px;
            animation: badgeBlink 1.8s ease-in-out infinite;
        }
        @keyframes badgeBlink {
            0%,
            100% {
                opacity: 1;
            }
            50% {
                opacity: 0.6;
            }
        }

        /* ============ 模态框 ============ */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.55);
            z-index: 9999;
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            pointer-events: none;
            transition: opacity 0.3s;
        }
        .modal-overlay.active {
            opacity: 1;
            pointer-events: all;
        }
        .modal {
            background: #fff;
            border-radius: 12px;
            width: 92%;
            max-width: 850px;
            max-height: 85vh;
            box-shadow: var(--shadow-lg);
            display: flex;
            flex-direction: column;
            transform: translateY(30px) scale(0.95);
            transition: transform 0.35s cubic-bezier(0.22, 0.61, 0.36, 1);
            overflow: hidden;
        }
        .modal-overlay.active .modal {
            transform: translateY(0) scale(1);
        }
        .modal-header {
            padding: 16px 22px;
            background: var(--primary);
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: space-between;
            flex-shrink: 0;
        }
        .modal-header h2 {
            font-size: 18px;
            font-weight: 700;
            letter-spacing: 0.4px;
            display: flex;
            align-items: center;
            gap: 8px;
        }
        .modal-close {
            width: 34px;
            height: 34px;
            border-radius: 50%;
            border: none;
            background: rgba(255, 255, 255, 0.2);
            color: #fff;
            font-size: 20px;
            cursor: pointer;
            transition: all 0.2s;
            display: flex;
            align-items: center;
            justify-content: center;
            line-height: 1;
        }
        .modal-close:hover {
            background: rgba(255, 255, 255, 0.35);
            transform: rotate(90deg);
        }
        .modal-body {
            padding: 20px 22px;
            overflow-y: auto;
            flex: 1;
            min-height: 300px;
        }
        /* 年份选择标签 */
        .year-tabs {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }
        .year-tab {
            padding: 10px 22px;
            border-radius: 25px;
            border: 2px solid #dde3ea;
            background: #fff;
            cursor: pointer;
            font-weight: 600;
            font-size: 14px;
            transition: all 0.25s;
            color: var(--text-light);
            white-space: nowrap;
        }
        .year-tab:hover {
            border-color: var(--primary);
            color: var(--primary);
            background: #f6f9fc;
        }
        .year-tab.active {
            background: var(--primary);
            color: #fff;
            border-color: var(--primary);
            box-shadow: 0 3px 10px rgba(26, 95, 158, 0.3);
        }
        /* 试卷区域 */
        .exam-paper {
            border: 1px solid #e0e4e8;
            border-radius: 8px;
            padding: 24px 20px;
            background: #fdfdfd;
            animation: fadeInUp 0.4s ease;
        }
        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(15px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        .exam-paper .exam-title {
            text-align: center;
            font-size: 18px;
            font-weight: 700;
            color: var(--text);
            margin-bottom: 6px;
            letter-spacing: 0.5px;
        }
        .exam-paper .exam-meta {
            text-align: center;
            font-size: 13px;
            color: var(--text-muted);
            margin-bottom: 18px;
            display: flex;
            justify-content: center;
            gap: 20px;
            flex-wrap: wrap;
        }
        .exam-paper .exam-meta span {
            background: #f0f4f8;
            padding: 4px 12px;
            border-radius: 12px;
        }
        .exam-paper hr {
            border: none;
            border-top: 1px dashed #dde3ea;
            margin: 16px 0;
        }
        .question-block {
            margin-bottom: 16px;
            padding: 10px 14px;
            background: #fafbfc;
            border-radius: 6px;
            border-left: 3px solid var(--primary);
        }
        .question-block .q-title {
            font-weight: 700;
            font-size: 14px;
            color: var(--primary-dark);
            margin-bottom: 6px;
        }
        .question-block .q-text {
            font-size: 14px;
            color: var(--text);
            line-height: 1.7;
            margin-bottom: 4px;
        }
        .question-block .q-options {
            font-size: 13px;
            color: var(--text-light);
            margin-left: 16px;
            line-height: 2;
        }
        .question-block .q-answer {
            font-size: 13px;
            color: #2e7d32;
            font-weight: 600;
            margin-top: 4px;
            background: #e8f5e9;
            display: inline-block;
            padding: 2px 10px;
            border-radius: 10px;
        }
        .download-btn {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            background: #27ae60;
            color: #fff;
            border: none;
            padding: 10px 20px;
            border-radius: 22px;
            cursor: pointer;
            font-size: 13px;
            font-weight: 600;
            margin-top: 16px;
            transition: all 0.25s;
            letter-spacing: 0.3px;
        }
        .download-btn:hover {
            background: #219a52;
            box-shadow: 0 4px 14px rgba(39, 174, 96, 0.35);
            transform: translateY(-1px);
        }

        /* ============ Toast提示 ============ */
        .toast {
            position: fixed;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%) translateY(80px);
            background: #2c3e50;
            color: #fff;
            padding: 12px 26px;
            border-radius: 30px;
            font-size: 14px;
            font-weight: 600;
            z-index: 99999;
            pointer-events: none;
            transition: transform 0.4s cubic-bezier(0.22, 0.61, 0.36, 1);
            letter-spacing: 0.4px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.3);
        }
        .toast.show {
            transform: translateX(-50%) translateY(0);
        }
        .toast.success {
            background: #27ae60;
        }

        /* ============ 响应式 ============ */
        @media (max-width: 900px) {
            .main-container {
                flex-direction: column;
                padding: 14px 12px 30px;
            }
            .sidebar-left {
                width: 100%;
            }
            .stats-grid {
                grid-template-columns: repeat(2, 1fr);
            }
            .nav-links li a {
                padding: 0 10px;
                font-size: 12px;
            }
            .nav-links li a.nav-zhenti {
                padding: 0 12px;
                font-size: 12px;
                margin: 8px 2px;
            }
            .modal {
                width: 97%;
                max-height: 90vh;
            }
        }

        @media (max-width: 600px) {
            .stats-grid {
                grid-template-columns: 1fr 1fr;
                gap: 8px;
            }
            .nav-links {
                gap: 0;
                flex-wrap: wrap;
                justify-content: center;
            }
            .nav-links li a {
                padding: 0 8px;
                font-size: 11px;
                height: 36px;
            }
            .header-inner {
                flex-wrap: wrap;
                height: auto;
                padding: 8px 12px;
                gap: 8px;
            }
            .logo-text {
                font-size: 14px;
            }
        }
    </style>
</head>
<body>

    <!-- ==================== 顶部信息条 ==================== -->
    <div class="top-bar">
        <div class="top-bar-inner">
            <span>📚 重庆工商大学 · 在线教学平台</span>
            <div class="user-info">
                <span style="font-size:12px;">欢迎您，</span>
                <div class="user-avatar">夏</div>
                <span style="font-weight:600;">夏</span>
                <button class="logout-btn" onclick="showToast('已安全退出（演示）')">退出</button>
            </div>
        </div>
    </div>

    <!-- ==================== Header & 导航 ==================== -->
    <header class="header">
        <div class="header-inner">
            <div class="logo-area">
                <div class="logo-icon">CTBU</div>
                <div>
                    <div class="logo-text">商务智能与应用实验</div>
                    <div class="logo-sub">管理科学与工程学院 · </div>
                </div>
            </div>
            <nav>
                <ul class="nav-links">
                    <li><a class="active" href="#" onclick="return false;">首页</a></li>
                    <li><a href="#" onclick="return false;">基本信息</a></li>
                    <li><a href="#" onclick="return false;">单元学习</a></li>
                    <li><a href="#" onclick="return false;">课程资源</a></li>
                    <li><a href="#" onclick="return false;">课程活动</a></li>
                    <!-- ★ 历年真题导航按钮 - 提案核心功能 ★ -->
                    <li><a class="nav-zhenti" href="javascript:void(0)" onclick="openZhentiModal()" title="点击查看历年真题试卷">
                            <span class="zt-icon">📋</span> 历年真题
                        </a></li>
                </ul>
            </nav>
        </div>
    </header>

    <!-- ==================== 主体内容 ==================== -->
    <div class="main-container">

        <!-- 左侧边栏 -->
        <aside class="sidebar-left">
            <!-- 教师信息 -->
            <div class="card">
                <div class="card-header">
                    <span class="icon-dot"></span> 教师信息
                </div>
                <div class="card-body">
                    <div class="teacher-name">👩‍🏫 </div>
                    <div class="teacher-dept">所属院系：学院</div>
                    <p class="teacher-bio">
                        重庆工商大学教授，国家自然科学基金项目评审专家。主讲课程；指导结题了*项本科生完成学生科技项目。
                    </p>
                    <a class="read-more" href="#" onclick="return false;">[阅读全文]</a>
                </div>
            </div>

            <!-- 课程介绍 -->
            <div class="card">
                <div class="card-header">
                    <span class="icon-dot"></span> 课程介绍
                </div>
                <div class="card-body">
                    <p>本课程旨在培养学生掌握的基本概念、方法和工具，能够运用。课程涵盖仓库。</p>
                    <a class="read-more" href="#" onclick="return false;">[阅读全文]</a>
                </div>
            </div>

            <!-- 最新动态 -->
            <div class="card">
                <div class="card-header">
                    <span class="icon-dot"></span> 最新动态
                </div>
                <div class="card-body" style="padding-top:8px;">
                    <ul class="news-list">
                        <li>发布了新的作业实验六销售数据分析</li>
                        <li>发布了新的作业实验五商品数据分析</li>
                        <li>发布了新的作业实验三市场和行业数据分析</li>
                        <li>发布了新的作业实验四竞争对手数据分析</li>
                        <li>发布了新的作业实验二商务数据的获取与管理</li>
                        <li>发布了新的作业实验一、初始商务数据</li>
                    </ul>
                </div>
            </div>
        </aside>

        <!-- 右侧内容区 -->
        <div class="content-right">
            <!-- 课程统计 -->
            <div class="stats-grid">
                <div class="stat-item">
                    <div class="stat-icon">👥</div>
                    <div class="stat-number">97</div>
                    <div class="stat-label">选课学生数</div>
                </div>
                <div class="stat-item">
                    <div class="stat-icon">👀</div>
                    <div class="stat-number">1,706</div>
                    <div class="stat-label">课程访问数</div>
                </div>
                <div class="stat-item">
                    <div class="stat-icon">🌐</div>
                    <div class="stat-number">28</div>
                    <div class="stat-label">访客访问数</div>
                </div>
                <div class="stat-item">
                    <div class="stat-icon">📢</div>
                    <div class="stat-number">0</div>
                    <div class="stat-label">课程通知数</div>
                </div>
                <div class="stat-item">
                    <div class="stat-icon">📁</div>
                    <div class="stat-number">20</div>
                    <div class="stat-label">课程资源数</div>
                </div>
                <div class="stat-item">
                    <div class="stat-icon">📝</div>
                    <div class="stat-number">10</div>
                    <div class="stat-label">课程作业数</div>
                </div>
            </div>

            <!-- ★★★ 历年真题功能卡片 - 提案演示核心 ★★★ -->
            <div class="zhenti-feature-card" onclick="openZhentiModal()" title="点击查看历年真题试卷">
                <span class="zhenti-badge">🆕 NEW</span>
                <div class="zhenti-feature-content">
                    <div class="zhenti-feature-icon">📋</div>
                    <div class="zhenti-feature-text">
                        <h3>📖 历年真题试卷</h3>
                        <p>提供往届期末考试真题，助力高效复习备考</p>
                    </div>
                    <span class="zhenti-feature-arrow">➤</span>
                </div>
            </div>

            <!-- 课程通知 -->
            <div class="card">
                <div class="card-header">
                    <span class="icon-dot"></span> 课程通知
                    <span style="font-size:12px;color:var(--text-muted);font-weight:400;">>> 更多</span>
                </div>
                <div class="card-body" style="padding-top:4px;">
                    <div class="notice-item"><span class="notice-tag">通知</span>期末考试安排已发布，请同学们及时查看</div>
                    <div class="notice-item"><span class="notice-tag">提醒</span>实验报告提交截止日期延长至第18周</div>
                    <div class="notice-item"><span class="notice-tag">公告</span>课程答疑时间调整：每周三下午2:00-4:00</div>
                </div>
            </div>

            <!-- 额外信息 -->
            <div class="card">
                <div class="card-header">
                    <span class="icon-dot"></span> 其他信息
                </div>
                <div class="card-body" style="font-size:13px;color:var(--text-light);line-height:2;">
                    课程视频资源数：<strong>0</strong> ｜ 课程讨论区主题数：<strong>0</strong><br>
                    课程讨论区发文数：<strong>0</strong> ｜ 常见问题数：<strong>0</strong><br>
                    课程问卷数：<strong>0</strong> ｜ 研究型教学：<strong>0</strong>
                </div>
            </div>
        </div>
    </div>

    <!-- ==================== 历年真题模态框 ==================== -->
    <div class="modal-overlay" id="zhentiModalOverlay">
        <div class="modal">
            <div class="modal-header">
                <h2>📋 历年真题试卷 — 商务智能与应用实验</h2>
                <button class="modal-close" onclick="closeZhentiModal()" title="关闭">✕</button>
            </div>
            <div class="modal-body" id="zhentiModalBody">
                <!-- 动态填充 -->
            </div>
        </div>
    </div>

    <!-- Toast -->
    <div class="toast" id="toast"></div>

    <script>
        // ============ 历年真题数据 ============
        const examData = {
            '2024': {
                year: '2024',
                semester: '2023-2024学年第二学期',
                title: '商务智能与应用实验 期末考试试卷（A卷）',
                time: '120分钟',
                score: '100分',
                questions: [
                    { type: '一、单项选择题',
                        items: [
                            '1. 数据仓库的核心特征是（  ）\n    A. 面向主题、集成、时变、非易失    B. 面向过程、分散、稳定、易失\n    C. 面向应用、独立、动态、易失    D. 面向对象、集中、静态、非易失',
                            '2. OLAP的中文含义是（  ）\n    A. 在线事务处理    B. 在线分析处理    C. 离线分析处理    D. 实时数据挖掘',
                            '3. 下列哪种图表最适合展示数据随时间的趋势变化？（  ）\n    A. 饼图    B. 散点图    C. 折线图    D. 雷达图',
                            '4. K-Means算法属于（  ）\n    A. 分类算法    B. 聚类算法    C. 回归算法    D. 关联规则算法',
                        ],
                        answer: '参考答案：1.A  2.B  3.C  4.B' },
                    { type: '二、判断题',
                        items: [
                            '1. 数据挖掘可以发现数据中隐藏的模式和规律。（  ）',
                            '2. ETL代表Extract-Transform-Load三个过程。（  ）',
                            '3. 决策树只能用于分类任务，不能用于回归任务。（  ）',
                        ],
                        answer: '参考答案：1.✓  2.✓  3.✕（决策树也可用于回归）' },
                    { type: '三、简答题',
                        items: [
                            '1. 简述商务智能（BI）系统的典型架构组成。（10分）',
                            '2. 解释数据清洗的重要性，并列举常见的脏数据类型。（10分）',
                            '3. 对比说明监督学习与无监督学习的主要区别。（10分）',
                        ],
                        answer: '评分要点：1.数据源层→ETL层→数据仓库层→分析层→展示层  2.缺失值/重复值/异常值/不一致格式等  3.有无标签数据、目标不同、典型算法不同' },
                ]
            },
            '2023': {
                year: '2023',
                semester: '2022-2023学年第二学期',
                title: '商务智能与应用实验 期末考试试卷（A卷）',
                time: '120分钟',
                score: '100分',
                questions: [
                    { type: '一、单项选择题',
                        items: [
                            '1. Apriori算法主要用于（  ）\n    A. 分类    B. 聚类    C. 关联规则挖掘    D. 降维',
                            '2. 在数据可视化中，热力图通常用于表示（  ）\n    A. 层级关系    B. 矩阵数据的密度分布    C. 时间序列    D. 比例关系',
                            '3. RFM模型中，F代表（  ）\n    A. 最近一次消费时间    B. 消费频率    C. 消费金额    D. 客户忠诚度',
                            '4. Python中进行数据分析最常用的库是（  ）\n    A. NumPy    B. Pandas    C. Matplotlib    D. Scikit-learn',
                        ],
                        answer: '参考答案：1.C  2.B  3.B  4.B' },
                    { type: '二、判断题',
                        items: [
                            '1. 数据标准化可以消除不同量纲对分析结果的影响。（  ）',
                            '2. 过拟合是指模型在训练集上表现差，在测试集上表现好。（  ）',
                        ],
                        answer: '参考答案：1.✓  2.✕（过拟合是训练集好、测试集差）' },
                    { type: '三、简答题',
                        items: [
                            '1. 请阐述数据仓库与操作型数据库的主要区别。（10分）',
                            '2. 举例说明关联规则挖掘在零售业中的应用。（10分）',
                        ],
                        answer: '评分要点：1.用途/数据组织/时间特性/更新方式不同  2.购物篮分析、捆绑销售、货架布局优化等' },
                ]
            },
            '2022': {
                year: '2022',
                semester: '2021-2022学年第二学期',
                title: '商务智能与应用实验 期末考试试卷（A卷）',
                time: '120分钟',
                score: '100分',
                questions: [
                    { type: '一、单项选择题',
                        items: [
                            '1. 下列哪项不属于数据挖掘的主要任务？（  ）\n    A. 分类    B. 聚类    C. 数据录入    D. 回归',
                            '2. Tableau是一款（  ）\n    A. 数据库管理系统    B. 数据可视化工具    C. 编程语言    D. 操作系统',
                            '3. 查准率（Precision）的计算公式是（  ）\n    A. TP/(TP+FP)    B. TP/(TP+FN)    C. TN/(TN+FP)    D. TP/(TP+TN)',
                        ],
                        answer: '参考答案：1.C  2.B  3.A' },
                    { type: '二、简答题',
                        items: [
                            '1. 描述CRISP-DM数据挖掘标准流程的六个阶段。（12分）',
                            '2. 结合实例说明数据可视化在商业决策中的价值。（13分）',
                        ],
                        answer: '评分要点：1.业务理解→数据理解→数据准备→建模→评估→部署  2.直观呈现/快速洞察/辅助沟通/支持决策' },
                ]
            },
        };

        let currentYear = '2024';

        /** 打开历年真题模态框 */
        function openZhentiModal(year = '2024') {
            currentYear = year;
            const overlay = document.getElementById('zhentiModalOverlay');
            overlay.classList.add('active');
            renderExamContent(year);
            document.body.style.overflow = 'hidden';
            // 记录演示操作
            console.log('✅ [提案演示] 用户点击了"历年真题"功能，当前查看年份：' + year);
        }

        /** 关闭模态框 */
        function closeZhentiModal() {
            const overlay = document.getElementById('zhentiModalOverlay');
            overlay.classList.remove('active');
            document.body.style.overflow = '';
            showToast('历年真题面板已关闭');
        }

        /** 渲染试卷内容 */
        function renderExamContent(year) {
            const data = examData[year];
            if (!data) return;
            const body = document.getElementById('zhentiModalBody');

            let questionsHTML = '';
            data.questions.forEach(q => {
                questionsHTML += `
                        <div class="question-block">
                            <div class="q-title">${q.type}</div>
                            ${q.items.map(item => `<div class="q-text" style="white-space:pre-line;">${item}</div>`).join('')}
                            ${q.answer ? `<span class="q-answer">${q.answer}</span>` : ''}
                        </div>
                    `;
            });

            body.innerHTML = `
                    <!-- 年份选择 -->
                    <div class="year-tabs">
                        <button class="year-tab ${year === '2024' ? 'active' : ''}" onclick="switchYear('2024')">📅 2024年真题</button>
                        <button class="year-tab ${year === '2023' ? 'active' : ''}" onclick="switchYear('2023')">📅 2023年真题</button>
                        <button class="year-tab ${year === '2022' ? 'active' : ''}" onclick="switchYear('2022')">📅 2022年真题</button>
                    </div>

                    <!-- 试卷 -->
                    <div class="exam-paper">
                        <div class="exam-title">${data.title}</div>
                        <div class="exam-meta">
                            <span>📅 ${data.semester}</span>
                            <span>⏱ 考试时间：${data.time}</span>
                            <span>📝 满分：${data.score}</span>
                            <span>📋 闭卷考试</span>
                        </div>
                        <hr>
                        ${questionsHTML}
                        <div style="text-align:center;margin-top:16px;">
                            <button class="download-btn" onclick="showToast('📥 模拟下载：${data.title}.pdf （提案演示功能）')">
                                📥 下载PDF版本
                            </button>
                        </div>
                    </div>
                `;

            // 滚动到顶部
            body.scrollTop = 0;
        }

        /** 切换年份 */
        function switchYear(year) {
            currentYear = year;
            renderExamContent(year);
            showToast('已切换至 ' + year + ' 年真题');
            console.log('✅ [提案演示] 切换至' + year + '年真题试卷');
        }

        /** Toast 提示 */
        function showToast(message) {
            const toast = document.getElementById('toast');
            toast.textContent = message;
            toast.className = 'toast success show';
            clearTimeout(toast._timeout);
            toast._timeout = setTimeout(() => {
                toast.classList.remove('show');
            }, 2000);
        }

        /** 点击遮罩关闭 */
        document.getElementById('zhentiModalOverlay').addEventListener('click', function(e) {
            if (e.target === this) {
                closeZhentiModal();
            }
        });

        /** 键盘ESC关闭 */
        document.addEventListener('keydown', function(e) {
            if (e.key === 'Escape') {
                const overlay = document.getElementById('zhentiModalOverlay');
                if (overlay.classList.contains('active')) {
                    closeZhentiModal();
                }
            }
        });

        /** 页面加载完成 */
        console.log('✅ 重庆工商大学课程页面（演示版）已就绪');
        console.log('📋 提案核心功能："历年真题"已集成到导航栏和功能卡片区');
        console.log('💡 点击导航栏【历年真题】按钮或橙色功能卡片即可查看演示效果');
        console.log('🔗 此网页可用于PPT超链接演示');
    </script>
</body>
</html>